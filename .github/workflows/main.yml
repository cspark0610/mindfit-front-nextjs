name: Cypress tests

# Controls when the workflow will run
on:
  # Triggers the workflow on pull request events but only for the dev branch
  pull_request:
    branches: [dev]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      # Install NPM dependencies, cache them correctly
      # and run all Cypress tests
      - name: Cypress run
        uses: cypress-io/github-action@v2
        env:
          NEXTAUTH_URL: http://localhost:3000
          SECRET: p/brjw5iCKJjHAyq5AOS42hC53srJr3FvKhZzNpjY/g=
          BASE_API_URL: http://52.87.178.165:5000/graphql
          CYPRESS_RECORD_KEY: 3c67b30b-0d63-4e43-9b41-75d9715d15bb
        with:
          build: yarn build
          start: yarn start
          record: true
