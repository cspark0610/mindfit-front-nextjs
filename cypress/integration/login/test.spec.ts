describe('login', () => {
  it('verify login with credentials', () => {
    // @ts-ignore
    cy.loginWithCredentials()
  })
})

describe('signout', () => {
  it('verify signout', () => {
    // @ts-ignore
    cy.logout()
  })
})
